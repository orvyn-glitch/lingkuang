修改项目中的 `index.html`（灵框 v3 阈限梦核设计系统的世界观时间线界面）。只改动时间线视图部分，其余界面和整体设计系统（tokens、配色、字体、动效）保持不变。

时间线界面当前是竖轴列表，请重做为一个 **Obsidian 风格横向节点图** 时间线：

## 新交互规格

1. **横向主时间线**：一条水平线贯穿视图，节点（事件点）分布在这条线上，按时间先后从左到右排列。节点是圆形/胶囊形标记，悬浮于线上下两侧交替排布（避免拥挤），每个节点显示名称 + 年份。

2. **世纪刻度**：时间线下方标注世纪/年代刻度（如 公元前1000年、公元0年、公元500年、公元1000年…），像地图比例尺一样均匀或按比例分布，浅色 mono 字体小字标注。

3. **节点展开**：点击节点标记 → 视图**水平平移动画**（平滑 scroll/transform），把该节点移到屏幕**左侧约 1/4 处**；同时**右侧滑出一个详情面板**（保持"壁龛"内凹风格），展示该节点的完整信息：标题、时间、描述正文、标签、关联人物/地点（占位字段）。

4. **再次点击同一节点或关闭按钮** → 详情面板收起，视图可平滑回到起点。

5. **多时间线管理**：界面顶部（或侧边栏内）有"时间线"切换区，可**自由创建新时间线**（命名即可），在多个时间线之间切换（每个时间线独立保存自己的节点数据，用 JS 对象按 id 存储，切换时重渲染）。当前时间线名高亮显示，旁边有"+"新建按钮和删除按钮。

6. **示例数据**：预置一条示例时间线「维斯莱特三世界」（用户的世界观项目名），含 4-6 个节点覆盖至少两个世纪跨度，中文命名（如「上古之门」公元前800年、「裂隙时代」公元312年、「三界分立」公元980年、「静默期」公元1450年…），让效果立即可见。另预置一条空时间线「新世界线」展示空状态文案（沿用设计系统的诗意空状态风格）。

## 约束

- 严格沿用 `design-system/tokens.css` 的变量与 DESIGN.md 规范：暖灰米底、冷荧光绿 accent（每屏≤2处）、内凹壁龛、VHS 颗粒、serif 标题/sans 正文/mono 时间戳、300ms+ 梦境缓动、无纯黑纯白、无饱和蓝
- 节点标记 hover 时有荧光绿角光（沿用在卡片上的手法）
- 点击节点的时间线平移用 smooth 滚动动画（CSS scroll-behavior 或 JS transform transition），约 640ms 梦境缓动
- 保持单文件自包含（内联 CSS + JS），双击浏览器可开
- 完成后用 Node 校验 HTML 标签平衡（注意 PowerShell 读 UTF-8 中文会乱码，用 node .cjs 脚本校验）
- 输出确认时列出：新交互点、示例数据、校验结果