你是一名资深 UI 设计师。项目「灵框 v3」(Lingkuang Toolbox v3) 是一个 AI 驱动的插件化桌面工具箱（Electron + React），用户是 18 岁的美术生（INFP），痴迷阈值空间（Liminal Space）、梦核（Dreamcore）、后室（Backrooms）美学。

项目目录里已有完整设计系统，必须先阅读：
- `design-system/DESIGN.md` — 设计语言完整规范（颜色、字体、组件、动效、反模式）
- `design-system/tokens.css` — 全部 CSS 变量（直接复制进产物）

请生成一个自包含的 HTML 文件 `index.html`，包含三个界面（通过左侧导航切换）：

1. **工具网格主页（The Lobby）**：侧边栏（The Seam，深色 chrome 220px，含导航）+ 主区为插件网格，工具卡片是"墙上的壁龛"（recessed 内凹风格，非浮起卡片）。首批三个工具：🌍 世界观时间线、📝 文本编辑器、⚙️ 系统工具。顶部有搜索框。AI 入口带荧光绿呼吸动效。
2. **世界观时间线界面**：时间线编辑视图，节点增删改，可视化时间轴（支持年份/事件节点）。
3. **AI 聊天面板（The Hum）**：浮动的暗色窗口，AI 消息左侧有荧光绿边缘线，AI 头像用后室黄 serif 字形"灵"，呼吸动效 8s 周期。

严格遵循 DESIGN.md 的所有规范：
- 颜色只用 tokens.css 里的变量，暖灰米底 #c5c2ba、冷荧光绿 #9ec262 点缀（每屏 ≤2 处）、后室黄 #dfc073、锈棕危险色
- 深度用内凹（inset shadow）+ 模糊，禁止硬阴影浮起
- 有 VHS 颗粒/扫描线 overlay 氛围层
- 中文界面，标题用宋体系 serif，正文用无衬线，时间戳用 mono
- 动效缓慢如呼吸（300ms+），有"苏醒"入场动画（卡片逐个淡入上浮）
- 文案遵循"安静、观察者、略带幽魂感"：空状态如"这里暂时空着。你走后，灯会开多久？"，AI 问候"我在。"
- 不要用饱和蓝、纯黑 #000、纯白 #fff、庆祝感元素

产出单个 index.html（内联所有 CSS），可直接双击在浏览器打开预览。